// Basic Hello, World! program

#include <stdio.h>
// allows usage of a collection of functions relating to terminal inputs and outputs

int main(void) {
    // This prints "Hello, World!" to the terminal, and the next terminal
    // command is printed on the line below it, instead of on the same
    // line.
    // What does this imply about what '\n' does?
    printf("Hello, World!\n");
    
    return 0;
    // return 0 means the code executed without any errors
}
